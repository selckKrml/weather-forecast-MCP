startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      city_name:
        type: string
        description: "Hava durumu sorgulanacak şehir adı"
    required: [city_name]
  commandFunction: |-
    (config) => ({ command: 'python', args: ['-m', 'server'] })
  exampleConfig:
    city_name: "Ankara"

